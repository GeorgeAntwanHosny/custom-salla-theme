{% extends 'layouts/master' %}

{% block seo %}
    <title>{{ trans('pages.cart') }} — {{ store.name }}</title>
{% endblock %}

{% block content %}
<div class="container" style="padding: 3rem 1.5rem;">
    <h1 class="section-title" style="margin-bottom:2rem;">{{ trans('pages.cart') }}</h1>
    {# Salla handles the full cart UI via web component #}
    <salla-cart></salla-cart>
</div>
{% endblock %}
