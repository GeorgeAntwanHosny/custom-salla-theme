{% extends 'layouts/master' %}

{% block seo %}
    <title>{{ trans('pages.loyalty') }} — {{ store.name }}</title>
{% endblock %}

{% block content %}
<div class="container" style="padding: 3rem 1.5rem;">
    <h1 class="section-title" style="margin-bottom:2rem;">{{ trans('pages.loyalty') }}</h1>
    <salla-loyalty></salla-loyalty>
</div>
{% endblock %}
